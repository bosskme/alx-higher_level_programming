#!/usr/bin/python3
def number_keys(a_dictionary):
    """
    This function takes a dictionary as input returns the number of keys.

    Parameters:
    a_dictionary (dict): A dictionary to count the number of keys.

    Returns:
    int: The number of keys in the dictionary.
    """
    return len(a_dictionary)
